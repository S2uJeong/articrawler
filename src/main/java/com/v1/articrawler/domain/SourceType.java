package com.v1.articrawler.domain;

/** Which kind of interest condition produced an article: a provider category/section, a search keyword, or a direct press RSS feed. */
public enum SourceType {
  CATEGORY,
  KEYWORD,
  PRESS
}
