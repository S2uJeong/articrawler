### Articrawler version 1.0.1 build
- 언론사별 필터링 기능 추가
- 
수정 내용: 언론사 필터가 이제 "우리가 등록한 7개 피드"가 아니라 실제 수집된 기사에 찍힌 모든 언론사명(총 166개, MBC뉴스·머니투데이 포함)을 보여주도록 바꿨습니다. GET
/api/stats/sources라는 새 엔드포인트를 추가해서 언론사별 기사 수를 집계하고, 드롭다운에 "MBC 뉴스 (8)"처럼 건수까지 표시해서 뭐가 많이 잡히는지 한눈에 보이게        
했습니다. 실제로 "MBC 뉴스" 선택해서 8건 정상적으로 필터링되는 것까지 확인했습니다.

한 가지 알아두실 점: MBC뉴스·머니투데이처럼 저희가 직접 RSS로 등록하지 않은 곳들은 전부 Google News 경유로만 들어오기 때문에, 필터링은 되지만 이미지는 계속          
플레이스홀더로 남습니다 (Google 링크 특성상 어쩔 수 없는 부분, 이전에 설명드린 것과 동일한 이유입니다). 실제 사진까지 보고 싶으시면 이 언론사들도 직접 RSS 피드로    
등록해야 하는데, 원하시면 머니투데이 경제 전용 피드나 MBC뉴스 RSS가 있는지 조사해서 추가해드릴 수 있습니다.

### 버그 수정 - 새 키워드 추가 후 수집이 안 되던 문제 (2026-09-15)
증상: '최수정' 키워드를 새로 추가했는데 기사가 하나도 안 모이고 수집 내역에도 아예 안 뜨는 문제가 있었습니다.

원인: 키워드 하나 문제가 아니라, 그보다 먼저 처리되는 다른 키워드(이 경우 "AI") 수집 중에 우연히 링크가 아주 긴(1241자) 기사가 섞여 들어왔는데, `Article.link`
컬럼이 `VARCHAR(1000)`으로 제한돼 있어서 저장이 실패했습니다. 이 저장 실패(`DataIntegrityViolationException`)는 코드에서 건별로 try/catch로 잡아서 넘어가게
되어 있었지만, 트랜잭션 자체는 이미 "rollback-only"로 오염된 상태라 해당 키워드의 수집 트랜잭션이 커밋되는 순간 `UnexpectedRollbackException`이 터졌고, 이게
전체 수집 루프(`NewsCollectionService.collectAll()`)를 그대로 중단시켜 버렸습니다. 그래서 등록 순서상 그 뒤에 있던 '최수정' 키워드는 아예 시도조차 되지 못한
것이었습니다. 즉 기사 링크 하나가 너무 길면 그 뒤의 모든 키워드/언론사 피드 수집이 조용히 통째로 멈추는 구조적 버그였습니다.

수정 내용:
1. `Article.link` 컬럼 길이를 1000 → 2048로 늘려서 이런 케이스 자체가 애초에 실패하지 않도록 했습니다.
2. 기사 저장을 `ArticleWriter`라는 별도 빈으로 분리하고 `@Transactional(propagation = REQUIRES_NEW)`로 감싸서, 기사 한 건 저장이 실패해도 그 저장 트랜잭션만
   롤백되고 나머지 배치(다른 키워드·언론사 피드 수집)는 영향받지 않도록 구조를 바꿨습니다. 앞으로 (링크 길이가 아닌) 다른 이유로 개별 기사 저장이 실패하더라도
   전체 수집이 멈추는 일은 없습니다.

재현/검증: 수정 후 서버 재시작 → 스케줄 수집이 자동으로 돌면서 '최수정' 키워드로 기사 100건이 정상 수집됐고, 그 뒤 언론사 피드(연합뉴스·뉴시스 등) 수집까지 중단 없이
이어지는 것을 확인했습니다.   